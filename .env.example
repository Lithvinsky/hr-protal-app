# Optional. Leave unset locally: the dev server proxies /api to Express on port 5000.
# On Vercel, set to your deployed API origin (no trailing slash), e.g. https://your-api.onrender.com
REACT_APP_API_URL=
