# Local dev: leave empty — CRA proxies /api to http://localhost:5000.
#
# Vercel (required for Production & Preview): set to your Express API origin
# with NO trailing slash, e.g. https://hr-api.onrender.com
# Login calls:  {REACT_APP_API_URL}/api/employees/login
REACT_APP_API_URL=
